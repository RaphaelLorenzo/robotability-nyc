# Python 3.11 (notebooks were run on 3.11.9)
# Core
numpy>=1.24
pandas>=2.0,<3
matplotlib>=3.7
scipy>=1.10
tqdm
pyarrow
jinja2

# Geospatial (GeoPandas <1 keeps the deprecated `op=` / unary_union APIs used in notebooks)
geopandas>=0.14,<1.0
shapely>=2.0
pyproj>=3.5
rasterio>=1.3
osmnx>=1.9
rtree
pyogrio
fiona
centerline

# Parallel GeoPandas/pandas apply in feature_processing
pandarallel

# Jupyter notebooks
jupyter
ipykernel
notebook

# Interactive NYC deployment map (video_materials/nyc_deployment)
shiny
